<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
         https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>Smart Campus API</display-name>
    <description>RESTful API for Smart Campus Sensor and Room Management</description>

    <!--
        NO explicit Jersey servlet declaration here.

        Why: When you declare the Jersey ServletContainer in web.xml AND your
        ApplicationConfig class has @ApplicationPath("/api/v1"), the two
        path definitions conflict. Jersey in servlet-mapping mode ignores
        @ApplicationPath and uses the url-pattern (/api/*) instead, so every
        resource ends up at the wrong path — causing HTTP 404 errors.

        The correct approach for GlassFish 7 (Jakarta EE 10):
          1. Leave web.xml clean (no Jersey servlet entry).
          2. Let GlassFish's built-in ServletContainerInitializer scan the WAR,
             find ApplicationConfig (extends ResourceConfig / Application)
             annotated with @ApplicationPath("/api/v1"), and register the
             Jersey servlet automatically at that path.
          3. All endpoints are then reachable under:
               http://localhost:8080/smart-campus-api/api/v1/

        Result:
          GET  http://localhost:8080/smart-campus-api/api/v1            → Discovery
          GET  http://localhost:8080/smart-campus-api/api/v1/rooms      → Rooms
          GET  http://localhost:8080/smart-campus-api/api/v1/sensors    → Sensors
    -->

    <welcome-file-list>
        <welcome-file>index.html</welcome-file>
    </welcome-file-list>

    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>

</web-app>
